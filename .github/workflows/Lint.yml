name: Lint

on: [push]

jobs:
  Lint:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Install dependencies
      run: |
        pip install poetry
        make install
          
    - name: Lint
      run: |
        make lint
